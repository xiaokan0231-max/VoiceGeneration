# 复制为 .env 后按需修改（gateway 会读取 VG_API_TOKEN）
# 开启本地鉴权（留空 = 不鉴权，仅本机使用）
VG_API_TOKEN=

# 本机 MySQL 历史记录（默认使用独立 voice_generation 数据库）
VG_DATABASE_URL=mysql+pymysql://root@127.0.0.1:3306/voice_generation?charset=utf8mb4
